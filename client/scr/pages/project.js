// TODO: this is purly an example for the format of the page 
    // After I code the format I need to take the information from past projects and 
    // create a folder where each one will use this format of code but under a dif project name

// Import react to iniatiallize page
import React from 'react';
// Import components from material ui

// add/delete as needed
import Box from '@mui/material/Box';
import CssBaseline from '@mui/material/CssBaseline';
import Container from '@mui/material/Container';
import Typography from '@mui/material/Typography';
import Button from '@mui/material/Button';


function Project(){
    return(
      <React.Fragment>
    </React.Fragment>
    );
}

export default Project; 